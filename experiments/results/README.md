# Local experiment evidence

This directory contains **compact, Git-tracked** summaries produced manually by `python run_local_experiments.py`.

Each UTC run gets its own directory. Large artifacts do not belong here; the runner places local-only working output under ignored `.petra_local/`.

Petra currently has no hosted CI. These files are human-triggered laptop evidence, not CI artifacts.
